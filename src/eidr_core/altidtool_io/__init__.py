"""AltIDTool 5-column TSV read/write (planned; not yet extracted).

Format: id, type, value, domain, relation (blank relation = IsSameAs).

Seeds: eidr-wikidata ``bmr/altidtool.py`` (emitter + validation gate),
EIDR MCP ``eidrtoaltid.py``, BMRtoAltID's emitter, and the remediation
tooling's action-TSV readers.
"""
